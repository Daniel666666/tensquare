server:
  port: 12000
spring:
  application:
    name: tensquare-config
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/Daniel520/tensquare_config.git
  rabbitmq:
    host: 192.168.109.128
management:
  endpoints:
    web:
      exposure:
        include: bus-refresh